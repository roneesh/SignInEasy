= form_for @organization_user, remote: true, authenticity_token: true do |f|
  = f.label :organization_id
  = f.text_field :organization_id

  = f.label :user_id
  = f.text_field :user_id

  = f.submit